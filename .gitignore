# --- Konfiguracja lokalna (maszynowa) ---
# .env i user_settings.json opisują KONKRETNY komputer: klucze API, ścieżki,
# wybrane urządzenia audio. Wzorce bez wartości leżą obok jako *.example.
.env
.env.local
.env.*.local
config/user_settings.json
config/dependency_status.json
config/offline_bundle.json
config/*.tmp

# Osobiste ustawienia narzędzi deweloperskich: zawierają ścieżki bezwzględne
# z maszyny autora. Wersja współdzielona (.claude/settings.json) może być
# wersjonowana — ta z ".local" nigdy.
.claude/settings.local.json

# --- Dane i modele ---
# Modele Whispera, głosy Pipera, embeddingi i modele RVC. Setki MB do kilku GB,
# pobierane skryptem (scripts/prepare_offline.py), nigdy trzymane w repozytorium.
models/

# Te same pliki, gdyby ktoś położył je POZA models/ — zabezpieczenie przed
# przypadkowym `git add` modelu głosu, którego i tak nie wolno rozprowadzać
# (patrz sekcja o prawach w README).
*.onnx
*.pth
*.gguf
*.safetensors
*.ckpt

# --- Cudzy kod pobierany skryptem (Applio), nie nasz i nie wersjonowany ---
# Klon repozytorium Applio razem z jego wagami embeddera i predyktorów.
# Pobiera je scripts/install-applio.sh; to setki MB cudzej własności.
third_party/

# --- Magazyn kół pip do instalacji bez internetu (duży, budowany lokalnie) ---
vendor/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
*.sqlite3.v*.bak
# Awaryjna lokalizacja bazy pamięci, gdy system nie poda katalogu danych
# (kontener bez HOME, usługa) — patrz config.app_data_directory().
data/

# --- Logi ---
logs/
*.log

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# --- Środowiska wirtualne ---
venv/
.venv/
env/
# Osobne środowisko dla RVC (Faza 15): biblioteki RVC nie działają na Pythonie
# nowszym niż 3.10, więc mieszkają obok, w drugim venvie. Kilka GB torcha
# i ścieżki bezwzględne z tej maszyny — nigdy do repozytorium.
.venv-rvc/
# Środowisko Applio (szybszy backend RVC). Applio przypina torcha 2.11 i
# wymaga Pythona 3.12+, więc nie mieści się ani w .venv, ani w .venv-rvc.
# Około 7 GB samego torcha i bibliotek CUDA — nigdy do repozytorium.
.venv-applio/
ENV/

# --- Pliki tymczasowe ---
*.tmp
*.bak
*.orig
*.rej
*.swp
*.swo
*~

# --- Edytory i systemy operacyjne ---
.idea/
.vscode/
.DS_Store
Thumbs.db
desktop.ini
